# 🏗️ بنية المشروع - Architecture

## 📁 هيكل المجلدات

```
├── convex/                    # Backend (Convex)
│   ├── _generated/           # ملفات Convex المولدة تلقائياً
│   ├── constants.ts          # الثوابت المشتركة
│   ├── schema.ts             # تعريفات قاعدة البيانات
│   ├── auth.ts               # المصادقة
│   ├── stats.ts              # الإحصائيات اليومية
│   ├── weeklyStats.ts        # الإحصائيات الأسبوعية
│   ├── monthlyStats.ts       # الإحصائيات الشهرية
│   ├── statCategories.ts     # إدارة التصنيفات
│   ├── statTopics.ts         # إدارة المواضيع
│   ├── userManagement.ts     # إدارة المستخدمين
│   ├── healthCenters.ts     # المراكز الصحية
│   ├── campaigns.ts          # الحملات الصحية
│   ├── activities.ts          # الأنشطة
│   ├── posters.ts            # البوسترات
│   ├── notifications.ts       # الإشعارات
│   ├── dashboard.ts          # لوحة التحكم
│   └── managerAnalytics.ts   # تحليلات المدير
│
├── src/
│   ├── app/                  # نقطة الدخول الرئيسية
│   │   └── App.tsx           # المكون الرئيسي
│   │
│   ├── features/             # الميزات (Feature-based structure)
│   │   ├── auth/            # المصادقة
│   │   │   ├── SignInForm.tsx
│   │   │   └── SignOutButton.tsx
│   │   │
│   │   ├── dashboard/       # لوحات التحكم
│   │   │   ├── Dashboard.tsx
│   │   │   └── ManagerDashboard.tsx
│   │   │
│   │   ├── statistics/      # الإحصائيات
│   │   │   ├── DailyStatsForm.tsx
│   │   │   ├── WeeklyStats.tsx
│   │   │   ├── MonthlyStats.tsx
│   │   │   ├── ManagerReview.tsx
│   │   │   └── ManageTopics.tsx
│   │   │
│   │   ├── campaigns/       # الحملات الصحية
│   │   │   └── Campaigns.tsx
│   │   │
│   │   ├── activities/      # الأنشطة
│   │   │   └── Activities.tsx
│   │   │
│   │   ├── health-centers/  # المراكز الصحية
│   │   │   └── HealthCenters.tsx
│   │   │
│   │   ├── posters/         # البوسترات
│   │   │   ├── Posters.tsx
│   │   │   └── PosterGallery.tsx
│   │   │
│   │   ├── reports/         # التقارير
│   │   │   └── Reports.tsx
│   │   │
│   │   └── admin/           # إدارة النظام
│   │       ├── UserManagement.tsx
│   │       ├── UserRegistration.tsx
│   │       └── SuperAdminSetup.tsx
│   │
│   ├── shared/              # الكود المشترك
│   │   ├── components/      # مكونات مشتركة
│   │   │   ├── LandingPage.tsx
│   │   │   └── NotificationBell.tsx
│   │   ├── types/           # تعريفات الأنواع
│   │   │   └── index.ts
│   │   ├── constants/       # الثوابت
│   │   │   └── index.ts
│   │   └── utils/           # وظائف مساعدة
│   │       └── index.ts
│   │
│   ├── components/          # مكونات UI الأساسية (Shadcn)
│   │   └── ui/
│   │       ├── button.tsx
│   │       ├── input.tsx
│   │       └── tabs.tsx
│   │
│   ├── lib/                 # مكتبات ووظائف مساعدة
│   │   ├── utils.ts         # وظائف Tailwind (cn)
│   │   ├── exportToExcel.ts # تصدير Excel
│   │   ├── dailyStatsExporter.ts
│   │   ├── reportExporter.ts
│   │   └── reportPrinter.ts
│   │
│   ├── main.tsx             # نقطة الدخول
│   └── index.css           # الأنماط العامة
│
├── public/                  # الملفات الثابتة
│   ├── kirkuk-logo.png
│   └── manifest.json
│
└── [config files]          # ملفات الإعدادات
```

## 🎯 مبادئ التصميم

### 1. Feature-Based Structure
- كل ميزة في مجلدها الخاص تحت `features/`
- كل ميزة تحتوي على مكوناتها وملفاتها ذات الصلة
- يسهل الصيانة والتوسع

### 2. Shared Resources
- `shared/` يحتوي على الكود المستخدم في أكثر من ميزة
- `types/` للتعريفات المشتركة
- `constants/` للثوابت
- `utils/` للوظائف المساعدة

### 3. Separation of Concerns
- **Frontend**: `src/features/` و `src/shared/`
- **Backend**: `convex/`
- **UI Components**: `src/components/ui/`
- **Utilities**: `src/lib/`

## 📦 الاستيراد (Imports)

### من نفس الميزة
```typescript
import { Component } from "./Component";
```

### من ميزة أخرى
```typescript
import { Component } from "../other-feature/Component";
```

### من shared
```typescript
import { UserRole } from "../../shared/types";
import { APP_NAME } from "../../shared/constants";
import { formatDateArabic } from "../../shared/utils";
```

### من lib
```typescript
import { exportToExcel } from "../../lib/exportToExcel";
```

### من convex
```typescript
import { api } from "../../convex/_generated/api";
```

## 🔧 إضافة ميزة جديدة

1. إنشاء مجلد جديد في `src/features/`
2. إضافة المكونات والملفات ذات الصلة
3. إذا كانت هناك أنواع أو ثوابت مشتركة، أضفها في `shared/`
4. استخدم `convex/` للوظائف الخلفية

## 📝 ملاحظات مهمة

- **لا تضع ملفات في `src/app/`** - هذا المجلد محجوز للملف الرئيسي فقط
- **استخدم `shared/` للكود المشترك** - لا تكرر الكود
- **حافظ على البنية الهرمية** - كل ميزة في مجلدها
- **استخدم TypeScript types** - من `shared/types/` عندما يكون ذلك ممكناً

## 🚀 التطوير المستقبلي

هذه البنية قابلة للتوسع بسهولة:
- إضافة ميزات جديدة: مجلد جديد في `features/`
- إضافة أنواع مشتركة: تحديث `shared/types/`
- إضافة ثوابت: تحديث `shared/constants/`
- إضافة وظائف مساعدة: تحديث `shared/utils/`

